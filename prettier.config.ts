import type { Config } from "prettier";

/**
 * Конфигурация prettier
 */
const config: Config = {
  semi: false,
  quoteProps: "consistent",
  singleQuote: true,
  jsxSingleQuote: true,
};

export default config;
